{
  "name": "flight-finder-server",
  "version": "1.0.0",
  "description": "Server for flight controller web app",
  "main": "index.js",
  "scripts": {
    "client-build": "cd client && npm run build",
    "start": "npm run client-build && node ./server/index.js",
    "local": "node ./server/index.js",
    "server": "node ./server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bennett Schoonerman",
  "license": "ISC",
  "dependencies": {
    "amadeus": "^8.0.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.41.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.27.5"
  }
}
